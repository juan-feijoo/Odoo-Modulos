<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_report_pos_order_search_inherit_vendor" model="ir.ui.view">
        <field name="name">report.pos.order.search.inherit.vendor</field>
        <field name="model">report.pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_report_pos_order_search"/>
        <field name="arch" type="xml">
            <!-- Apuntamos a la etiqueta <search> que SIEMPRE existe -->
            <xpath expr="//search" position="inside">
                <filter string="Proveedor Principal" name="group_by_main_vendor" context="{'group_by':'main_vendor_id'}"/>
            </xpath>
        </field>
    </record>
</odoo>